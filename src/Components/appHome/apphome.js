import React from 'react'

// components
import LandingPage from '../langingpage/'

export default function AppHome(props) {
    return (
        <React.Fragment>
            <LandingPage/>  
        </React.Fragment>
    )
}
